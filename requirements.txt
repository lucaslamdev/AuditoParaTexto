# Dependências do Assistente de Voz Offline (CPU).
# Instalação padrão (CPU):
#   pip install -r requirements.txt
#
# GPU NVIDIA (opcional):
#   Instale o stack CUDA conforme a documentação do faster-whisper / ctranslate2.
#   Depois configure DEVICE=cuda e WHISPER_COMPUTE=float16 ou int8.

sounddevice
soundfile
numpy
pynput
pyperclip
vosk
faster-whisper

# Interface gráfica (janela glass via PyWebView).
# No Windows, requer o runtime Microsoft Edge WebView2 instalado
# (baixe em https://developer.microsoft.com/microsoft-edge/webview2/).
pywebview
